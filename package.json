{
  "name": "learning-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:data": "python3 scripts/generate_manifest.py",
    "build:study-books": "python3 scripts/generate_study_book_index.py",
    "generate:seo": "node scripts/generate-seo-pages.mjs",
    "start": "npm run build && python3 -m http.server --directory dist 4173",
    "dev": "vite",
    "build": "npm run build:study-books && vite build && npm run generate:seo",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "marked": "^18.0.3"
  }
}
