# Stage 1-3: listing scraper, matcher, downloader
playwright>=1.44.0
pandas>=2.0.0
python-dateutil>=2.9.0
requests>=2.31.0
tqdm>=4.66.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# Stage 4: PDF → Markdown converter (aqa_pdf_to_md.py)
pdfplumber>=0.11.0        # primary text extraction
pymupdf>=1.24.0           # fallback block-level extraction (import as fitz)
pdf2image>=1.17.0         # OCR fallback: convert PDF pages to images
pytesseract>=0.3.13       # OCR fallback: Tesseract wrapper
Pillow>=10.0.0            # required by pdf2image

# Tests
pytest>=8.0.0
